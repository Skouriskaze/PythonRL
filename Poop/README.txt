The game of POOP.

classes.py contains all the classes needed to play the game.
use Game(players) to create a new game, where players is a list of players.

The game waits for a Game.play_card() to be called. It only progresses if the current
player submits a move.

Game also has a GameState, which can be accessed with Game.gamestate.
